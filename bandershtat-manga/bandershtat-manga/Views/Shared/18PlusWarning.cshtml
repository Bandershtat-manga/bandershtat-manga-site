<!-- Views/Shared/18PlusWarning.cshtml -->

<style>
    #ageWarning {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(64, 64, 64, 0.9); /* Прозорий білий фон */
        z-index: 1000;
        text-align: center;
    }

    #ageWarningContent {
        padding: 20px;
        display: inline-block;
    }

    #ageWarning p{
        color: #FFF;
        text-align: center;
        font-family: Inter;
        font-size: 22px;
        font-style: normal;
        font-weight: 400;
        line-height: normal;
        padding: 3%
    }

    #confirmAgeButton {
        border-radius: 21px;
        background-color: red;
        color: white;
        padding: 1%;
        margin-right: 5%
    }

    #cancelButton {
        border-radius: 21px;
        background-color: rgb(20, 17, 17);
        color: white;
        padding: 1%;
    }

    #img {
        width: 20%;
        height: 20%;
    }

    #buttons{
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
    }

</style>

<div id="ageWarning">
    <div id="ageWarningContent">
        <img src="https://imagizer.imageshack.com/img922/9901/VeBSJf.png" id="img" />
        <p>Сайт може містити інформацію, призначену для осіб старше 18 років. Щоб продовжити, підтвердьте, що ви досягли цього віку.</p>
        <div id="buttons">
            <button id="confirmAgeButton">Підтвердити</button>
            <button id="cancelButton">Відміна</button>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        var ageWarning = document.getElementById('ageWarning');
        var confirmButton = document.getElementById('confirmAgeButton');
        var cancelButton = document.getElementById('cancelButton');

        // Перевірте, чи користувач вже підтверджував вік раніше
        var ageConfirmation = localStorage.getItem('ageConfirmation');
        if (!ageConfirmation) {
            // Показати попередження, якщо вік ще не підтвердено
            ageWarning.style.display = 'block';
        }

        // Обробники подій
        confirmButton.addEventListener('click', function () {
            ageWarning.style.display = 'none';
            // Зберегти інформацію про підтвердження в локальному сховищі
            localStorage.setItem('ageConfirmation', 'true');
        });

        cancelButton.addEventListener('click', function () {
            // Можливо, виконати дії для випадку відміни, наприклад, перенаправити користувача на іншу сторінку або викинути з сайту
            window.location.href = "https://www.example.com"; // Змініть URL на бажаний
        });
    });
</script>
